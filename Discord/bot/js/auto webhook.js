const webhookclient = rrriite(discord)const whooks = "link"setInterval(async message => {  var webhooks = whooks.split("\n");  for (var i = 0; i < webhooks.length; i++) {    var hook = new WebhookClient(      webhooks[i].split("/")[5],      webhooks[i].split("/")[6]    );          hook.send("Test webhook", {            username: "webhook bot",           // avatarURL: " "  }}, 15000);